# JS-Practice
Java Script Learning

In JS go any browser and click inspect we got a console, there if we run any code where we get the output we also get a prototype
if we click the prototype we get multiple function about that.